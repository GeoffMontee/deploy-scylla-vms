---
- name: Install exact Scylla node-exporter without bind, stack, or startup
  hosts: scylla
  gather_facts: true
  become: true
  strategy: linear
  serial: 1
  any_errors_fatal: true
  max_fail_percentage: 0
  tags:
    - monitoring-agent

  pre_tasks:
    - name: Validate exact monitoring-agent install intent and supported guest
      ansible.builtin.assert:
        that:
          - inventory_hostname == deploy_scylla_vms_logical_id
          - deploy_scylla_vms_role == 'scylla'
          - deploy_scylla_vms_host_key_checking_required is sameas true
          - deploy_scylla_vms_monitoring_agent is mapping
          - >-
            deploy_scylla_vms_monitoring_agent.schema_version ==
            'deploy-scylla-vms.ansible-monitoring-agent/v1'
          - >-
            deploy_scylla_vms_monitoring_agent.logical_id ==
            inventory_hostname
          - deploy_scylla_vms_monitoring_agent.channel == 'stable'
          - deploy_scylla_vms_monitoring_agent.release_line == '2026.2'
          - >-
            deploy_scylla_vms_monitoring_agent.listen_policy ==
            'not-started'
          - >-
            deploy_scylla_vms_monitoring_agent.service_unit ==
            'scylla-node-exporter'
          - >-
            deploy_scylla_vms_monitoring_agent.packages ==
            ['scylla-node-exporter']
          - >-
            deploy_scylla_vms_monitoring_agent.documented_metrics_port
            == 9100
          - >-
            deploy_scylla_vms_monitoring_agent.configuration_performed
            is sameas false
          - >-
            deploy_scylla_vms_monitoring_agent.process_exporter_installed
            is sameas false
          - >-
            deploy_scylla_vms_monitoring_agent.stack_installed
            is sameas false
          - >-
            deploy_scylla_vms_monitoring_agent.targets_generated
            is sameas false
          - >-
            deploy_scylla_vms_monitoring_agent
            .manager_registration_performed
            is sameas false
          - >-
            deploy_scylla_vms_monitoring_agent.scylla_started
            is sameas false
          - >-
            deploy_scylla_vms_monitoring_agent.secrets_written
            is sameas false
          - >-
            deploy_scylla_vms_monitoring_agent.package_version
            is match(
              '^2026[.]2[.](0|[1-9][0-9]*)-0[.][0-9]{8}'
              '[.][0-9a-f]{12}-1$'
            )
          - >-
            deploy_scylla_vms_monitoring_agent.image_operating_system ==
            'Ubuntu'
          - >-
            deploy_scylla_vms_monitoring_agent.image_operating_system_version ==
            '24.04'
          - ansible_facts.distribution == 'Ubuntu'
          - ansible_facts.distribution_version == '24.04'
          - ansible_facts.os_family == 'Debian'
          - >-
            (
              deploy_scylla_vms_monitoring_agent.architecture == 'amd64'
              and ansible_facts.architecture == 'x86_64'
            )
            or
            (
              deploy_scylla_vms_monitoring_agent.architecture == 'aarch64'
              and ansible_facts.architecture == 'aarch64'
            )
          - >-
            deploy_scylla_vms_monitoring_agent.signing_key.fingerprint ==
            '6C6ECC84F42AF147BD2A65AEC503C686B007F39E'
          - >-
            deploy_scylla_vms_monitoring_agent.signing_key.artifact_digest ==
            'sha256:f2f1f4368a71820ea1f5e4e8900e59ddeec1e78cf98450f3ac84826c828ea417'
          - >-
            deploy_scylla_vms_monitoring_agent.signing_key.resource ==
            'content/playbooks/roles/scylla_install/files/scylladb-2026.asc'
        fail_msg: >-
          Monitoring agent installation requires current exact Ubuntu 24.04,
          architecture, Scylla install, version, and provenance evidence.
        quiet: true
      changed_when: false
      no_log: true
      tags:
        - always
        - preflight

    - name: Emit truthful check-mode result
      ansible.builtin.debug:
        msg: >-
          DSV_MONITORING_AGENT_B64={{
            {
              'schema_version':
                'deploy-scylla-vms.ansible-monitoring-agent/v1',
              'logical_id': inventory_hostname,
              'status': 'not-predicted',
              'requested_release':
                deploy_scylla_vms_monitoring_agent.release_line,
              'requested_version':
                deploy_scylla_vms_monitoring_agent.package_version,
              'installed_version': none,
              'packages': {},
              'repository_digest':
                deploy_scylla_vms_monitoring_agent
                .repository.definition_digest,
              'signing_key_digest':
                deploy_scylla_vms_monitoring_agent
                .signing_key.artifact_digest,
              'signing_key_fingerprint':
                deploy_scylla_vms_monitoring_agent
                .signing_key.fingerprint,
              'service_enabled': none,
              'service_inactive': none,
              'listen_policy': 'not-started',
              'configuration_performed': false,
              'process_exporter_installed': false,
              'stack_installed': false,
              'targets_generated': false,
              'manager_registration_performed': false,
              'scylla_started': false,
              'secrets_written': false,
              'provenance':
                deploy_scylla_vms_monitoring_agent.provenance,
              'blockers': []
            }
            | to_json
            | b64encode
          }}
      when: ansible_check_mode
      changed_when: false
      tags:
        - always

    - name: Stop after non-predictive check result
      ansible.builtin.meta: end_host
      when: ansible_check_mode
      tags:
        - always

  tasks:
    - name: Create the owner-controlled APT keyring directory
      ansible.builtin.file:
        path: /etc/apt/keyrings
        state: directory
        owner: root
        group: root
        mode: "0755"
      tags:
        - packages

    - name: Install the vendored authenticated ScyllaDB 2026 signing key
      ansible.builtin.copy:
        src: roles/scylla_install/files/scylladb-2026.asc
        dest: /etc/apt/keyrings/scylladb-2026.asc
        owner: root
        group: root
        mode: "0644"
      register: monitoring_agent_key
      tags:
        - packages

    - name: Configure the exact signed ScyllaDB 2026.2 repository
      ansible.builtin.deb822_repository:
        name: scylladb-2026-2
        types:
          - deb
        uris:
          - >-
            {{
              deploy_scylla_vms_monitoring_agent.repository.uri
            }}
        suites:
          - stable
        components:
          - main
        architectures:
          - >-
            {{
              'amd64'
              if deploy_scylla_vms_monitoring_agent.architecture == 'amd64'
              else 'arm64'
            }}
        signed_by: /etc/apt/keyrings/scylladb-2026.asc
        enabled: true
        state: present
        mode: "0644"
      register: monitoring_agent_repository
      tags:
        - packages

    - name: Install the exact allowlisted node-exporter package
      ansible.builtin.apt:
        name: >-
          {{
            deploy_scylla_vms_monitoring_agent.packages
            | map(
              'regex_replace',
              '$',
              '='
              + deploy_scylla_vms_monitoring_agent.package_version
            )
            | list
          }}
        state: present
        update_cache: true
        allow_downgrade: false
        force: false
        force_apt_get: true
        install_recommends: false
        policy_rc_d: 101
      register: monitoring_agent_packages
      tags:
        - packages

    - name: Keep the node-exporter disabled and inactive
      ansible.builtin.systemd_service:
        name: scylla-node-exporter
        enabled: false
        state: stopped
        daemon_reload: true
      register: monitoring_agent_service
      tags:
        - verify

    - name: Gather installed package facts
      ansible.builtin.package_facts:
        manager: apt
      changed_when: false
      no_log: true
      tags:
        - verify

    - name: Initialize normalized package evidence
      ansible.builtin.set_fact:
        monitoring_agent_installed_packages: {}
      changed_when: false
      no_log: true
      tags:
        - verify

    - name: Build normalized package evidence
      ansible.builtin.set_fact:
        monitoring_agent_installed_packages: >-
          {{
            monitoring_agent_installed_packages
            | combine({
              item: (
                ansible_facts.packages[item]
                | map(attribute='version')
                | unique
                | list
                | first
              )
            })
          }}
      loop: "{{ deploy_scylla_vms_monitoring_agent.packages }}"
      when:
        - item in ansible_facts.packages
        - >-
          ansible_facts.packages[item]
          | map(attribute='version')
          | unique
          | list
          | length == 1
      changed_when: false
      no_log: true
      tags:
        - verify

    - name: Verify the exact installed package set and versions
      ansible.builtin.assert:
        that:
          - >-
            monitoring_agent_installed_packages.keys()
            | list
            | sort
            ==
            deploy_scylla_vms_monitoring_agent.packages
          - >-
            monitoring_agent_installed_packages.values()
            | unique
            | list
            ==
            [deploy_scylla_vms_monitoring_agent.package_version]
          - >-
            'prometheus-process-exporter'
            not in ansible_facts.packages
          - "'process-exporter' not in ansible_facts.packages"
        quiet: true
      changed_when: false
      no_log: true
      tags:
        - verify

    - name: Inspect node-exporter enablement
      ansible.builtin.command:
        argv:
          - /usr/bin/systemctl
          - is-enabled
          - scylla-node-exporter.service
      register: monitoring_agent_service_enabled
      changed_when: false
      failed_when: false
      no_log: true
      tags:
        - verify

    - name: Inspect node-exporter activity
      ansible.builtin.command:
        argv:
          - /usr/bin/systemctl
          - is-active
          - scylla-node-exporter.service
      register: monitoring_agent_service_active
      changed_when: false
      failed_when: false
      no_log: true
      tags:
        - verify

    - name: Refuse package-script startup or service enablement
      ansible.builtin.assert:
        that:
          - monitoring_agent_service_enabled.stdout == 'disabled'
          - monitoring_agent_service_active.stdout == 'inactive'
        quiet: true
      changed_when: false
      no_log: true
      tags:
        - verify

  post_tasks:
    - name: Emit strict normalized monitoring-agent install evidence
      ansible.builtin.debug:
        msg: >-
          DSV_MONITORING_AGENT_B64={{
            {
              'schema_version':
                'deploy-scylla-vms.ansible-monitoring-agent/v1',
              'logical_id': inventory_hostname,
              'status': (
                'installed'
                if (
                  monitoring_agent_key.changed
                  or monitoring_agent_repository.changed
                  or monitoring_agent_packages.changed
                  or monitoring_agent_service.changed
                )
                else 'no-change'
              ),
              'requested_release':
                deploy_scylla_vms_monitoring_agent.release_line,
              'requested_version':
                deploy_scylla_vms_monitoring_agent.package_version,
              'installed_version':
                deploy_scylla_vms_monitoring_agent.package_version,
              'packages': monitoring_agent_installed_packages,
              'repository_digest':
                deploy_scylla_vms_monitoring_agent
                .repository.definition_digest,
              'signing_key_digest':
                deploy_scylla_vms_monitoring_agent
                .signing_key.artifact_digest,
              'signing_key_fingerprint':
                deploy_scylla_vms_monitoring_agent
                .signing_key.fingerprint,
              'service_enabled': false,
              'service_inactive': true,
              'listen_policy': 'not-started',
              'configuration_performed': false,
              'process_exporter_installed': false,
              'stack_installed': false,
              'targets_generated': false,
              'manager_registration_performed': false,
              'scylla_started': false,
              'secrets_written': false,
              'provenance':
                deploy_scylla_vms_monitoring_agent.provenance,
              'blockers': []
            }
            | to_json
            | b64encode
          }}
      changed_when: false
      no_log: false
      tags:
        - always
